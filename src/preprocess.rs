/// QUBO preprocessing functions

use crate::qubo::*;

/// NOTE:
/// This function serves as a usage hint in the greater project context.
/// Preprocessing rules 1-5
pub fn shrink(qubo: QuboInstance) -> QuboInstance {
    todo!();
}
